source "https://rubygems.org"

# Jekyll 4, built and deployed by GitHub Actions (.github/workflows/pages.yml).
# Don't add the `github-pages` gem: it pins Jekyll 3 and GitHub's plugin allowlist.
gem "jekyll", "~> 4.4"

# Jekyll auto-requires every gem in this group. Used in _includes/pf/head.html
# ({% seo %}). Mirrored under `plugins:` in _config.yml.
group :jekyll_plugins do
  gem "jekyll-seo-tag", "~> 2.8"
end
